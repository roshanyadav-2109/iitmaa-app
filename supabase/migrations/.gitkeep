# 0001_init.sql is now committed (reconstructed from the live PanIIT project
# on 2026-09-21). It was previously owner-provided and absent from the repo,
# which made a fresh Supabase project impossible to build from source.
